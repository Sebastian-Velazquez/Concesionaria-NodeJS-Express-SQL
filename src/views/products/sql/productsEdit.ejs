<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Editar Producto</h1>
    <form action="/product/edit/<%= producto.id_product %>?_method=PUT" method="POST">
        <div>
            <label>Nombre</label>
            <input type="text" name="name" value="<%= producto.name %>">
        </div>
        <div>
            <label>Precio</label>
            <input type="text" name="price" value="<%= producto.price %>">
        </div>
        <div>
            <label>Año</label>
            <input type="date" name="anio" value="<%= producto.date %>">
        </div>
        <div>
            <p>Descripción: <textarea name="description" ><%= producto.description %></textarea></p>
        </div>
        <div>
            <input type="file" name="image">
        </div>
        <div>
            <label>Color del auto</label>
                <select name="color">
                    <% for(i=0; i < colors.length; i++) {%>
                        <option value="<%= colors[i].id_color %>" selected>
                            <%= colors[i].nombre %>
                        </option>
                    <% } %>
                </select>
        </div>
        <div>
            <label>Selecionar modelo</label>
                <select name="models">
                    <% for(i=0; i < models.length; i++) {%>
                        <option value="<%= models[i].id_modelo %>" selected>
                            <%= models[i].tipo_de_modelo %>
                        </option>
                    <% } %>
                </select>
        </div>
        <div>
            <input type="submit">
        </div>
</body>
</html>